// Current version of the recording-consent language a parent agrees to.
// Bump this whenever the privacy policy's "Camera, Microphone & Session
// Recordings" section materially changes, so we can tell which version of the
// policy each parent actually consented under. Shared by the students and
// parent modules (kept here to avoid a circular import between them).
export const RECORDING_CONSENT_VERSION = '2026-07-01';
